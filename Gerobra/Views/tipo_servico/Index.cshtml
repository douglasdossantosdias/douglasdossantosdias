@model IEnumerable<Gerobra.Models.tipo_servico>

@{
    ViewBag.Title = "Tipo Serviço";
}

    <h2>Tipo Serviço</h2>

<p>
    @Html.ActionLink("Criar novo tipo de serviço", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_tipo_servico }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.id_tipo_servico }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.id_tipo_servico })
        </td>
    </tr>
}

</table>
