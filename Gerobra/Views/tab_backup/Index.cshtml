@model IEnumerable<Gerobra.Models.tab_backup>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome_arquivo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dta_backup)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sin_ativo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo_backup.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usuario.id_usuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome_arquivo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dta_backup)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sin_ativo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo_backup.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usuario.id_usuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tab_backup }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tab_backup }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tab_backup })
        </td>
    </tr>
}

</table>
