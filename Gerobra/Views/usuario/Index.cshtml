@model IEnumerable<Gerobra.Models.usuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sin_ativo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pessoa.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo_usuario.descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sin_ativo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pessoa.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo_usuario.descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_usuario }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_usuario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_usuario })
        </td>
    </tr>
}

</table>
