@model IEnumerable<Gerobra.Models.rel_cliente_obra>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cliente.id_cliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.obra.cod_obra)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cliente.id_cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.obra.cod_obra)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_rel_cliente_obra }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_rel_cliente_obra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_rel_cliente_obra })
        </td>
    </tr>
}

</table>
