@model IEnumerable<Gerobra.Models.status_obra>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sin_ativo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sin_ativo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_status_obra }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_status_obra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_status_obra })
        </td>
    </tr>
}

</table>
