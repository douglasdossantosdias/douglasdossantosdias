@model IEnumerable<Gerobra.Models.grau_instrucao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ordem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo_curso.nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ordem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo_curso.nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_grau_instrucao }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_grau_instrucao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_grau_instrucao })
        </td>
    </tr>
}

</table>
