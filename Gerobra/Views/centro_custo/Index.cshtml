@model IEnumerable<Gerobra.Models.centro_custo>

@{
    ViewBag.Title = "Centro de Custos";
}

<h2>Centro de Custos</h2>

<p>
    @Html.ActionLink("Criar Centro de Custos", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ativo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sintetico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sin_ativo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ativo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sintetico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sin_ativo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_centro_custo }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.id_centro_custo }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.id_centro_custo })
        </td>
    </tr>
}

</table>
