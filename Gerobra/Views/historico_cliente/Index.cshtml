@model IEnumerable<Gerobra.Models.historico_cliente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dta_historico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sin_ativo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cliente.id_cliente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dta_historico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sin_ativo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cliente.id_cliente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_historico_cliente }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_historico_cliente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_historico_cliente })
        </td>
    </tr>
}

</table>
