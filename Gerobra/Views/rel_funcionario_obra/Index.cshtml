@model IEnumerable<Gerobra.Models.rel_funcionario_obra>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.funcionario.id_funcionario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.obra.cod_obra)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.funcionario.id_funcionario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.obra.cod_obra)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_rel_funcionario_obra }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_rel_funcionario_obra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_rel_funcionario_obra })
        </td>
    </tr>
}

</table>
